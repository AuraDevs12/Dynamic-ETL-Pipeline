# Server
PORT=3000

# MongoDB connection URI (default local)
MONGODB_URI=mongodb://localhost:27017/dynamic_etl_demo

# Schema sampling and thresholds
SAMPLE_SIZE=50
SCHEMA_OPTIONAL_THRESHOLD=0.05

# Cron schedule for inference/normalization (every minute by default)
SCHEMA_CRON=*/1 * * * *

# Tesseract worker (if using system tesseract, tesseract.js will use it if available)
# No setting required here, but ensure Tesseract binary is installed for better OCR performance.
